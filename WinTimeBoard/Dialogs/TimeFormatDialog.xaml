<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="WinTimeBoard.Dialogs.TimeFormatDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinTimeBoard.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="时间格式设置"
    PrimaryButtonText="应用"
    CloseButtonText="取消"
    DefaultButton="Primary">

    <!-- 很奇怪，OverlayCornerRadius 属性一用上去就打不开，在 StackPanel 里加和在外面套 Grid 都一样 -->
    <StackPanel Spacing="16" MinWidth="350">
        <TextBlock Text="选择时间显示格式" FontSize="14" />
        
        <RadioButtons x:Name="TimeFormatRadioButtons" SelectionChanged="TimeFormatRadioButtons_SelectionChanged">
            <RadioButton Content="24小时制 (HH:mm:ss)" Tag="HH:mm:ss" IsChecked="True" />
            <RadioButton Content="24小时制 (HH:mm)" Tag="HH:mm" />
            <RadioButton Content="12小时制 (h:mm:ss tt)" Tag="h:mm:ss tt" />
            <RadioButton Content="12小时制 (h:mm tt)" Tag="h:mm tt" />
            <RadioButton Content="自定义格式" Tag="Custom" />
        </RadioButtons>
        
        <!-- 自定义格式输入 -->
        <StackPanel x:Name="CustomFormatPanel" Visibility="Collapsed" Spacing="8">
            <TextBox x:Name="CustomFormatTextBox" 
                     Header="自定义时间格式" 
                     PlaceholderText="例如: HH:mm:ss"
                     TextChanged="CustomFormatTextBox_TextChanged" />
            <TextBlock Text="格式说明：" FontSize="12" FontWeight="SemiBold" />
            <TextBlock FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                • HH: 24小时制小时 (00-23)
                <LineBreak/>
                • h: 12小时制小时 (1-12)
                <LineBreak/>
                • mm: 分钟 (00-59)
                <LineBreak/>
                • ss: 秒 (00-59)
                <LineBreak/>
                • tt: AM/PM 标识符
            </TextBlock>
        </StackPanel>
        
        <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}" />
        
        <!-- 预览区域 -->
        <StackPanel>
            <TextBlock Text="预览" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8" />
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                    CornerRadius="4" Padding="12"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1">
                <TextBlock x:Name="PreviewText" 
                           Text="12:34:56" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center" />
            </Border>
        </StackPanel>
    </StackPanel>
</ContentDialog>